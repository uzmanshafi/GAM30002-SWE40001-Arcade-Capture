using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StaffTower : Tower
{

    // Start is called before the first frame update
    void Start()
    {
        base.init();
    }

    protected override void tryShoot()
    {
    }

    // Update is called once per frame
    void Update()
    {

    }
}
